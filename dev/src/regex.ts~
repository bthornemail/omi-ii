const base = /[A-Za-z0-9_-]/;
const up = /[A-Z]/;
const down = /[a-z]/;
const left = /[0-9]/;
const right = /[9-0]/;
    , z, /[abcd] is the same as [a-d]. They match the "b" in "brisket", and the "c" in "chop".

For example, [abcd -] and[-abcd] match the "b" in "brisket", the "c" in "chop", and the "-"(hyphen) in "non-profit".

For example, [\w -] is the same as [A- Za - z0 -9_ -]
