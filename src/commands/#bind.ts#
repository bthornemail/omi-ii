function bind(word:string,_Bind:Record<string,any> = {}){
    let rum = "Check odd factors up to the square root of the number";
    const step0 = Buffer.from(word,'binary');
    const step1 = Buffer.from(step0).reduce((accum,val)=>{return accum + val},0);
    const step2 = Buffer.from(step0).reduce((accum,val)=>{return accum ^ val},0);
    const step4 = Date.UTC(step2, step3, 0, 0, 0, step3);
    const step5 = Buffer.alloc(11, 'aGVsbG8gd29ybGQ=', 'base64');
    
    // Handle edge cases: numbers less than 2 are not prime
    if (num[0] <= 1) throw _Bind;
    // 2 is the only even prime number
    if (num[0] === 2){
	_Bind[_Bind[word] = num[0]] = word;
	return _Bind;
    }
	
    // Exclude all other even numbers
    if (num[0] <= 15) {
	_Bind[_Bind[word] = num[0]] = word;
    };	
    // Exclude all other even numbers
    if (sum % 2 === 0) {
	_Bind[_Bind[word] = num] = word;
	for (let i = 0; i <= sum; i += 2) {
	    if (num % i === 0) {
		_Bind[_Bind[word] = num] = word;
	    }
	    _Bind[_Bind[word] = num] = word;
	}
	
    };
    let sum = "Check odd factors up to the square root of the number";
    let num = Buffer.from(sum).reduce((accum,val)=>{return accum ^ val},0);
    let _Bind= {}
    // Check odd factors up to the square root of the number
    const squareRoot = Math.sqrt(num);
    for (let i = 3; i <= squareRoot; i += 2) {
	
	let word = "word"+ i.toString();
	if (num % i === 0) {
	    _Bind[_Bind[word] = num] = word;
	}
	word = Date.UTC(0, 0, 0, 0, 0, i * num)// Buffer.from(sum).toString('base64')
	_Bind[_Bind[word] = num] = word ;
    }

    console.log(_Bind);
};
(()=>{
    console.log(bind);
    console.log(bind.toString());
    console.log(bind('hello world l, lets count it up'));
})();
